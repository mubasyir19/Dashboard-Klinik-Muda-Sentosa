<table id="example2" class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>No.</th>
      <th>Kategori</th>
      <th>Aksi</th>
    </tr>
  </thead>
  <tbody>
    <!-- <%# for(let i = 0; i < kls.length; i++) { %>
    <tr>
      <td><%#= i + 1 %></td>
      <td><%#= kls[i].nama %></td>
      <td>
        <form action="/kelas/hapus/<%#= kls[i].id %>?_method=DELETE" method="post">
          <a href="/kelas/edit/<%#= kls[i].id %>" class="btn btn-info btn-sm">
            <i class="fas fa-pencil-alt"></i>
            Ubah
          </a>
          <a
            href="javascript:void()"
            type="button"
            class="btn btn-danger btn-sm button-delete"
            data-id="<%#= kls[i].id %>"
          >
            <i class="fas fa-trash-alt"></i>
            Hapus
          </a>
        </form>
      </td>
    </tr>
    <%# } %> -->
  </tbody>
</table>
